Add resume parser prompt and update docs

This PR adds a reusable resume parser prompt (prompts/resume-parser.md) that extracts structured resume fields and returns both JSON (for programmatic use) and a short Markdown summary (for humans). It also updates README.md and prompts/README.md to list and document the new prompt.

Fixes #8
